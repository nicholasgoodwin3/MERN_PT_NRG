import React from 'react';

const Home = (props) => {
    return (
        <h1>Hello!</h1> //I couldnt figure out how to get this to only load when specifically called, instead of loading when using the background color as well.
    )
}
export default Home;